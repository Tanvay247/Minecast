version: "3.9"

services:
  rarity:
    build: ./rarity_service
    container_name: minecast-rarity
    ports:
      - "8001:8000"
    volumes:
      - ~/.cache:/root/.cache
    restart: unless-stopped
